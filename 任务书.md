# Codex 任务提示词：在无独显笔记本上最简运行 FPEM 单图颜值评分

## 任务目标

请在当前 Windows 项目目录中完成一个**最小可运行的命令行程序**，使用论文模型 **FPEM** 对一张已经由用户手动裁剪好的人脸 PNG/JPG 图片进行吸引力评分。

最终使用方式尽量简化为：

```powershell
python score.py "D:\图片\face.png"
```

终端输出示例：

```text
FPEM score: 3.4271 / 5.0000
```

只需要得到一个可信的模型原始评分，不需要 GUI，不需要自动裁脸，不需要批量处理。

---

## 运行设备与硬约束

设备：

- 华为 MateBook 14 2023
- Intel Core i5-1240P
- Intel 核显
- Windows x64
- **没有 NVIDIA 显卡**
- **不能依赖 CUDA**
- 只考虑 CPU 推理

必须满足：

1. 全程使用 CPU，禁止安装或要求 CUDA。
2. 单张图片推理即可，速度慢一些可以接受。
3. 不训练模型。
4. 不下载 LiveBeauty 数据集。
5. 不开发 GUI。
6. 不增加与单图评分无关的功能。
7. 尽量减少代码量、文件数量和依赖数量。
8. 优先保证真正运行成功，不要只生成未经执行验证的代码。

---

## 优先使用的官方项目

官方代码：

```text
https://github.com/Estella-LH/FPEM
```

论文：

```text
https://openaccess.thecvf.com/content/ICCV2025/html/Li_FPEM_Face_Prior_Enhanced_Facial_Attractiveness_Prediction_for_Live_Videos_ICCV_2025_paper.html
```

优先直接复用官方仓库中的：

- 模型结构；
- 官方预训练权重；
- 官方图片预处理；
- 官方最终分数计算方法。

不要自行编造“黄金比例算法”，也不要使用没有论文或没有公开权重来源的在线颜值 API。

---

## 实现要求

### 一、先检查官方代码

先阅读官方仓库，确认：

- 预训练权重的实际文件名和路径；
- 测试代码如何构建模型；
- 图片需要生成哪些尺寸或张量；
- 官方代码如何从模型输出计算最终 1～5 分；
- 哪些位置硬编码了 `.cuda()`、`torch.cuda.*` 或 CUDA AMP。

不要凭猜测重写网络结构。

### 二、最小化改造成 CPU 单图推理

实现 `score.py`，至少支持：

```powershell
python score.py IMAGE_PATH
```

可选支持：

```powershell
python score.py IMAGE_PATH --checkpoint CHECKPOINT_PATH
```

程序应当：

1. 检查图片是否存在；
2. 支持 `.png`、`.jpg`、`.jpeg`；
3. 正确处理 RGB 图片；
4. 对带透明通道的 PNG，先转换为普通 RGB；
5. 按官方测试流程完成 resize、padding、normalize 等预处理；
6. 以 `map_location="cpu"` 加载权重；
7. 使用 `model.eval()`；
8. 使用 `torch.inference_mode()` 或 `torch.no_grad()`；
9. 禁用或绕开所有 CUDA 专属代码；
10. 输出官方定义的最终分数；
11. 保证同一图片重复运行结果稳定。

不要擅自把 1～5 分换算成百分制，也不要额外进行百分位推断。

### 三、图片输入约定

用户会提前手动裁剪图片，因此：

- 不要求自动人脸检测；
- 不要求识别人脸框；
- 不要求 landmark；
- 假定图片包含一张完整、清晰的人脸；
- 脚本内部只做官方模型需要的预处理。

图片裁剪失败或图片中没有人脸，不需要复杂识别，只需在 README 中说明输入要求。

### 四、环境方案

优先选择最容易在 Windows CPU 上成功安装的方案。

可以：

- 使用 Conda 创建兼容环境；
- 根据官方代码兼容性选择 Python 3.8、3.9 或 3.10；
- 安装 CPU 版 PyTorch；
- 仅保留推理真正需要的依赖。

不要机械执行官方 CUDA 安装命令。

请输出以下二者之一：

```text
requirements_cpu.txt
```

或：

```text
environment.yml
```

如果某个旧依赖在 Windows 上难以安装，优先通过少量代码替换移除它，而不是为了完全复刻官方环境安装大量无关包。

### 五、权重处理

优先使用官方仓库已经提供的 FPEM 预训练权重。

要求：

- 自动在常见官方路径中查找权重，或允许用 `--checkpoint` 指定；
- 如果权重不在仓库内，README 给出准确下载位置和放置路径；
- 不把数百 MB 权重复制多份；
- 不重新训练；
- 不使用随机初始化模型冒充评分结果。

如果检查点键名与模型不完全一致，必须确认原因并正确处理，不能随意 `strict=False` 后忽略大量关键参数。若确实需要 `strict=False`，请打印缺失键和多余键并确认不影响核心模型。

---

## 最终需要保留的文件

尽量只保留：

```text
score.py
requirements_cpu.txt
README.md
```

如果必须复用官方仓库模块，可以保留必要的原始模块；不要大规模重构整个项目。

README 必须包含可直接复制执行的 Windows PowerShell 命令，例如：

```powershell
conda create -n fpem-cpu python=3.8 -y
conda activate fpem-cpu
pip install -r requirements_cpu.txt
python score.py ".\face.png"
```

README 还要说明：

- 权重放在哪里；
- 支持的图片格式；
- 图片应如何裁剪；
- 第一次加载可能较慢；
- 输出是模型对该照片的 1～5 分预测，不是客观颜值真值。

---

## 验收标准

任务完成前请自行实际验证以下项目：

1. 在没有 CUDA 的环境中可以启动。
2. `python score.py --help` 正常。
3. 输入不存在的路径时给出清晰报错。
4. 输入一张 PNG 时能够完成推理。
5. 输出为有限浮点数。
6. 正常情况下评分落在官方量表范围内，通常应为 1～5。
7. 同一张图片连续运行两次，结果一致或仅存在极小浮点误差。
8. 程序不会尝试调用 CUDA。
9. README 中的命令与实际文件一致。
10. 不依赖 LiveBeauty 数据集的 CSV、标注分数或人脸框文件。

完成后请汇报：

- 新增或修改了哪些文件；
- 实际使用的 Python、PyTorch 和关键依赖版本；
- 使用了哪个官方检查点；
- 实测单张图片 CPU 推理耗时；
- 实测输出示例；
- 是否对官方代码做了 CPU 兼容修改。

---

## 失败时的降级方案

首先必须认真尝试让官方 FPEM 检查点在 CPU 上完成单图推理。

只有在出现明确、可复现且短时间内无法解决的官方代码或权重缺陷时，才允许降级。降级时：

1. 先记录 FPEM 失败的完整技术原因和报错；
2. 改用有同行评议论文、公开数据集和公开预训练权重的 **SCUT-FBP5500** 官方模型；
3. 仍然实现相同的 CPU 单图命令行接口；
4. 输出时必须明确标注实际模型名称，不能仍写成 FPEM；
5. 不允许改用无论文依据的娱乐评分网站或第三方付费 API。

SCUT-FBP5500 官方仓库可作为降级参考：

```text
https://github.com/HCIILAB/SCUT-FBP5500-Database-Release
```

---

## 工作原则

- 先运行、再精简。
- 先复用官方推理逻辑，再考虑重写。
- 以最小改动解决 CPU 兼容问题。
- 不做界面，不做训练，不做批量系统。
- 不为“代码看起来完整”而加入无关抽象。
- 不能确认的模型细节必须从官方代码或论文核实。
- 最终目标只有一个：在这台无独显 Windows 笔记本上，输入一张手动裁剪的人脸图片，输出论文模型评分。
